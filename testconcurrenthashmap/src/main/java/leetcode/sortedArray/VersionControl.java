package leetcode.sortedArray;

public class VersionControl {

	static boolean isBadVersion(int version){
      System.out.println("coming here");
		if (version ==2 || version ==3 || version ==4) return true;
		return false;
	}
}
